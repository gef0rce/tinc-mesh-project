[Unit]
Description=tinc vpn
After=network.target

[Service]
Type=forking
ExecStart=/usr/sbin/tincd -n {{ netname }} -d3 --logfile
ExecReload=/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
